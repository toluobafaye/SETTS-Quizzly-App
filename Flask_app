from flask import Flask, render_templates, request, url_for, flash
from werkzeug.utilis import redirect
from flask_mysqlbd import MySQL
from flask import MySQL

app = flask(__name__)
app.secret_key = 'some_random_data'

@app.route('/')
def Index():
    return render_template('index.html')